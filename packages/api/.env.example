# Lokale Entwicklung (ohne Docker)
# =================================
# Diese Datei nur für "npm run dev" relevant.
# Für Docker: nutze die .env im Root-Verzeichnis.

# Datenbank-Pfad (SQLite)
DATABASE_URL="file:./recury.db"

# Login-Passwort
AUTH_PASSWORD=changeme

# Session-Secret
SESSION_SECRET=dev-secret-nicht-fuer-produktion

# API-Port
APP_PORT=3000

# Zeitzone
TZ=Europe/Berlin
